apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sso-frontend-phan-quan
  labels:
    name: assignment
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: sso-frontend-phan-quan.assignment.eks004.en.internal
      http:
        paths:
          - path: /?(.*)
            pathType: Prefix
            backend:
              service:
                name: sso-frontend-phan-quan
                port:
                  number: 3000
